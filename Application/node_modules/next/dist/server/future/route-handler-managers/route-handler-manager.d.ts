import type { BaseNextRequest } from 'next/dist/server/base-http';
import type { ModuleLoader } from 'next/dist/server/future/helpers/module-loader/module-loader';
import type { AppRouteRouteHandlerContext } from 'next/dist/server/future/route-modules/app-route/module';
import type { AppRouteRouteMatch } from 'next/dist/server/future/route-matches/app-route-route-match';
/**
 * RouteHandlerManager is a manager for route handlers.
 */
export type RouteHandlerManagerContext = AppRouteRouteHandlerContext;
export declare class RouteHandlerManager {
    private readonly moduleLoader;
    constructor(moduleLoader?: ModuleLoader);
    handle(match: AppRouteRouteMatch, req: BaseNextRequest, context: RouteHandlerManagerContext): Promise<Response>;
}
